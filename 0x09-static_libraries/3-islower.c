#include "main.h"

/**
 * _islower - the program checks for lowercase character
 * @c: character to check
 *
 * Retutn: 1 if c is lowercase, and 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
